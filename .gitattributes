# Auto-detect text files and normalize line endings
* text=auto

# Force LF for shell scripts
api/gradlew text eol=lf

# Force CRLF for Windows batch files
api/*.bat text eol=crlf

# Treat JARs as binary
*.jar binary
